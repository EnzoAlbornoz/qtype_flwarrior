define([], function() {

    return {
        setup_edit_tests_button: function() {
            alert("BRUH");
            const button = document.querySelector("[name=machine-test-add-test]");
            const test_list = document.querySelector("")
            const userFields = [];
            button.addEventListener("click", (ev) => {
                // Add New Option

            })
        }
    };
});
